apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: tomcat-test
  labels:
    app: tomcat-test
spec:
  # storageClassName: standard
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 80G
